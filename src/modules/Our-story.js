import { FontAwesomeIcon } from '@fortawesome/react-fontawesome'
import { faCoffee } from '@fortawesome/free-solid-svg-icons'
import { faBagShopping } from '@fortawesome/free-solid-svg-icons'

const itemStyle = {
    minWidth: '90px',
    marginRight: '30px'

}

const element = <FontAwesomeIcon icon={faBagShopping} />
function OurStory() {
  return (
    <div style={{color:'white',display:'flex', width:'100%', backgroundColor:'black', minHeight:'200px', justifyContent:'center'}}>
  
    </div>
  );
}

export default MiddleItems;